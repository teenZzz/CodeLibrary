<Window x:Class="CodeLibrary.Views.AddBookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:CodeLibrary.Views"
        mc:Ignorable="d"
        Background="#F6F6F6"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Title="CodeLibrary" Height="650" Width="620">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Добавить книгу"  VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="32" FontWeight="Bold" Margin="0 16 0 0"/>
        
        <TextBlock Grid.Row="1" Text="* - необязательные поля" Margin="16 0 0 10"/>
        
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock Text="Название книги :" FontSize="18" Margin="16 0 0  0" Width="160"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Width="400" Height="25" Margin="10 0 0 0 " FontSize="16"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock Text="Фамилия автора :" FontSize="18" Margin="16 0 0  0" Width="160"/>
            <TextBox Text="{Binding AuthorSurname, UpdateSourceTrigger=PropertyChanged}" Width="400" Height="25" Margin="10 0 0 0 " FontSize="16"/>
        </StackPanel>
        
        <StackPanel Grid.Row="4" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock Text="Имя Автора :" FontSize="18" Margin="16 0 0  0" Width="160"/>
            <TextBox Text="{Binding AuthorFirstName, UpdateSourceTrigger=PropertyChanged}" Width="400" Height="25" Margin="10 0 0 0 " FontSize="16"/>
        </StackPanel>
        
        <StackPanel Grid.Row="5" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock Text="Отчество автора* :" FontSize="18" Margin="16 0 0  0" Width="160"/>
            <TextBox Text="{Binding AuthorPatronymic, UpdateSourceTrigger=PropertyChanged}" Width="400" Height="25" Margin="10 0 0 0 " FontSize="16"/>
        </StackPanel>
        
        <StackPanel Grid.Row="6" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock Text="Выберите тэг :" FontSize="18" Margin="16 0 0  0" Width="160"/>
            <ComboBox ItemsSource="{Binding Tags}" SelectedValue="{Binding Tag, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="25" Margin="10 0 0 0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="7" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock Text="Описание* :" FontSize="18" Margin="16 0 0  0" Width="160"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="400" Height="25" Margin="10 0 0 0 " FontSize="16"/>
        </StackPanel>
        
        <StackPanel Grid.Row="8" VerticalAlignment="Center" HorizontalAlignment="Left" Orientation="Horizontal">
            <TextBlock Text="Статус :" FontSize="18" Margin="16 0 0  0" Width="160"/>
            <ComboBox ItemsSource="{Binding Statuses}" SelectedValue="{Binding Status, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="150" Height="25" Margin="10 0 0 0"/>
        </StackPanel>
        
        <Button Grid.Row="9" Command="{Binding AddBookCommand}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="180" Height="40" Content="Добавить" FontSize="18" Foreground="White" Background="#141414" BorderThickness="0" BorderBrush="Transparent">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            CornerRadius="12"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Наведение -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#2A2A2A"/>
                        </Trigger>

                        <!-- Нажатие -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="#3A3A3A"/>
                        </Trigger>

                        <!-- Выключена -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
